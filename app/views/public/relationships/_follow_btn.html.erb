<div>
  <% if current_user.following?(user) %>
    <%= link_to "フォロー外す", user_relationship_path(user.id), method: :delete, class: "btn btn-sm btn-delete" %>
  <% else %>
    <%= link_to "フォローする", user_relationships_path(user.id), method: :post, class: "btn btn-sm btn-default" %>
  <% end %>
</div>